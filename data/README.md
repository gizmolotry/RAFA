# Data

This folder holds local corpora, manifests, scraped source material, and other dataset-side inputs.

## Main areas
- `wav_files/`: local WAV corpus
- `datasets/`: processed/derived dataset helpers
- `scrapes/`: raw scraped source drops
- `input/`: manual inputs and staging files

Some original top-level data paths are preserved as hidden junctions for compatibility.
