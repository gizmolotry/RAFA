# Hypothesis Tracking
**Research Track: H2 (The Radical Claim)**

**H2:** "RAFA trained on audio learns a general relational mechanism that can solve logic tasks without borrowing semantics from language models."

**Constraint:** Do NOT use RoBERTa, BERT, or any frozen Transformer embeddings as the semantic input. 
**Input Modality:** Synthetic symbol systems or raw relational encodings projected directly into the continuous phase crystal.

---
## The "River Time" Ontology

1. **State:** $z(t) \in \mathbb{C}^N$ (Complex amplitudes on a lattice).
2. **Coupling (Fractal Access):** $C_	heta^{(k)}$ sparse matrices representing multiscale connections.
3. **Dynamics (The River):**
   $\dot{z} = \left(\sum_k \alpha_k C^{(k)}ight)z + i\Omega z - \gamma|z|^2 z + 
u\Delta z + 	ext{input}(t)$
4. **Dissipation (Irreversible Computation):** The nonlinear $-\gamma|z|^2 z$ term forces the system to forget weak structures and collapse into stable attractors (meaning).
5. **Memory:** Winding numbers, phase defects, and holonomy (not token lists).

---
## Experimental Milestones

**Phase 1 (Active): Path B Audio**
* Prove that interference and dissipation on a phase lattice can generate structured, coherent audio without mode collapse.

**Phase 2 (Pending): Synthetic Relational Logic**
* Create a synthetic, non-language dataset (e.g., temporal sequence completion, continuous constraint satisfaction).
* Project into the lattice using purely geometric encodings (no LLM embeddings).
* Train the multiscale couplings $\alpha_k$ to force the system into attractors corresponding to correct logical completions.
